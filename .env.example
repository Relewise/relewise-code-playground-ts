# To keep your .env across refreshes, log in to StackBlitz to persist it or add !.env to .gitignore.
# StackBlitz creates a public fork of the repo, so don't push secrets, like API keys.
VITE_DATASET_ID=YOUR_DATASET_ID_HERE
VITE_API_KEY=YOUR_API_KEY
VITE_SERVER_URL=YOUR_SERVER_URL
